import { db } from "./middleware/db";

export default async function editproducts(req, res) {
  //   db();
  //   const { id, type, model, brand, storageSpace, colors } = req?.body;
  //   const data = await Mobile.findbyId(id);
  //   data?.model=model
  //   return res.status(200).json({
  //     data,
  //     message: "Successfully Uploaded Mobile Model",
  //   });
}
